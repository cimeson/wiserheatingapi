version: 2.1

orbs:
  docker: circleci/docker@1.7.0

jobs:
  build-docker:
    docker:
      - image: cimg/base:stable
    steps:
      - docker/build:
          image: wiserheating 

workflows:
  example-workflow:
    jobs:
      - build-docker

version: 2.1

orbs:
  docker: circleci/docker@2.2.0
resource_class: medium

jobs:
  project-setup:
    docker:
      - image: cimg/base:stable
    steps:
      - setup_remote_docker
      - checkout

workflows:
  build-and-push:
    jobs:
      - project-setup
      - docker/publish:
          context:
            - docker
          image: $DOCKER_USERNAME/wiserheatingapi:circleci
          docker-password: DOCKER_PASSWORD
          extra_build_args: --build-arg ARCH=amd64
      - docker/publish:
          context:
            - docker
          image: $DOCKER_USERNAME/wiserheatingapi:circleci
          docker-password: DOCKER_PASSWORD
          extra_build_args: --build-arg ARCH=arm64
